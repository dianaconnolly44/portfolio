// Diana Connolly
// Section C, Tuesdays at 1:30
// dconnoll@andrew.cmu.edu
// Assignment-01

function setup() {
    createCanvas(600, 600);
}

function draw() {
	background("white");
	var c = color("red");  // Define color 'c'
	fill(c);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect( 0,0, 256.04, 323)
	var d = color("black");  // Define color 'c'
	fill(d);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect(256.04, 0, 12.96, 600)
	var d = color("black");  // Define color 'c'
	fill(d);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect(0, 323, 600, 12.96)
	var d = color("black");  // Define color 'c'
	fill(d);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect(546.718,335.96, 12.96,600)
	var e = color("blue");  // Define color 'c'
	fill(e);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect(558.678,335.96, 53.282,99.004)
	var d = color("black");  // Define color 'c'
	fill(d);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect(558.678, 434.964, 53.282, 20.017)
	var e = color("yellow");  // Define color 'c'
	fill(e);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect( 269, 580.992, 156.03,19.008)
	var d = color("black");  // Define color 'c'
	fill(d);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect(269, 568.032, 285, 12.96)
	var d = color("black");  // Define color 'c'
	fill(d);  // Use color variable 'c' as fill color
	noStroke();  // Don't draw a stroke around shapes
	rect(425.03, 581, 125,19.008)
}
